# Python dependencies for ReconEngine
# theHarvester — NOT on PyPI. Pre-installed in Docker via apt (theharvester).
# paramspider  — installed from GitHub source in Dockerfile.
aiohttp==3.11.14
pyyaml==6.0.2
wafw00f==2.2.0
python-nmap==0.7.1
jinja2==3.1.4
requests==2.32.3
dnspython==2.7.0
colorama==0.4.6
