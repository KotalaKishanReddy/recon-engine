# Python dependencies for ReconEngine
# theHarvester — NOT on PyPI. Pre-installed in Docker via apt (theharvester).
# paramspider  — install from GitHub directly (no stable PyPI release).
aiohttp==3.9.5
pyyaml==6.0.1
wafw00f==2.2.0
python-nmap==0.7.1
jinja2==3.1.4
requests==2.31.0
dnspython==2.6.1
colorama==0.4.6
